/*linked list reversal trial 1*/
int main(){
	static struct llist{
		int value;
		char *s;
		struct llist *next;
	};
	struct llist root=NULL;
	struct llist curr_node=root;
	do{
	printf("Enter key, value pair: \n");
	scanf("%d %s",curr_node.value,curr_node.s);
	curr_node
}while();

}